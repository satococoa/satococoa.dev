{
  "name": "satococoa.dev",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "pnpm build && wrangler deploy --config dist/server/wrangler.json",
    "upload": "pnpm build && wrangler versions upload --config dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.5",
    "@iconify-json/lucide": "^1.2.126",
    "astro": "^7.2.9",
    "astro-icon": "^1.2.0",
    "fast-xml-parser": "^5.11.1",
    "wrangler": "^4.127.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
